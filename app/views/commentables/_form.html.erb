<%= form_for(@commentable) do |f| %>
  <% if @commentable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commentable.errors.count, "error") %> prohibited this commentable from being saved:</h2>

      <ul>
      <% @commentable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :object_type_id %><br />
    <%= f.text_field :object_type_id %>
  </div>
  <div class="field">
    <%= f.label :local_id %><br />
    <%= f.text_field :local_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
